{

  // 求幂运算符（**）返回将第一个操作数加到第二个操作数的幂的结果。
  // 它等效于Math.pow，不同之处在于它也接受BigInts作为操作数。
  const a = Math.pow(3,4)//81
  console.log(a);
  const b = 3 ** 4
  console.log(b);

console.log(10 ** -2);     // 0.01

console.log(2 ** 3 ** 2);   // 512

console.log((2 ** 3) ** 2);  // 64



}
